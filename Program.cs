// Підключаємо базовий простір імен .NET
using System;

// Оголошуємо публічний інтерфейс. 
// За загальноприйнятим стандартом C# (Naming Conventions), назви всіх інтерфейсів починаються з великої літери "I" (IWordProvider).
public interface IWordProvider
{
    // Метод для отримання загаданого слова.
    // Зверніть увагу: тут немає фігурних дужок {} і тіла методу. 
    // Інтерфейс лише каже: "Будь-який клас, який мене використовує, ЗОБОВ'ЯЗАНИЙ мати метод GetWord(), що повертає рядок (string)".
    string GetWord();

    // Метод для отримання підказки до слова.
    // Так само: лише сигнатура методу. Логіка того, звідки береться підказка, буде написана в класі-спадкоємці.
    string GetHint();
}